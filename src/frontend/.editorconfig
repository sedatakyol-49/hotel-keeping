# Frontend ek kurallari — depo kokundeki `.editorconfig` gecerli kalir
# (`root = true` bilincli olarak yazilmadi ki kok ayarlar miras alinsin).

[*.ts]
quote_type = single
ij_typescript_use_double_quotes = false

[*.md]
max_line_length = off
trim_trailing_whitespace = false
